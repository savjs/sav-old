/*

import {Sav, Router, TplRender, VueRender, ContractLoader} from 'sav'

let sav = new Sav()

let router = new Router()

router.group({path:''}, plugins)
  .get({}, plugins)
  .get({}, plugins)
  .get({}, plugins)
  .group({path:'/account'}, plugins)
  .post({}, plugins)
  .post({}, plugins)

let tpl = new TplRender()

let ssr = new VueRender()

let contract = new ContractLoader()

 */
