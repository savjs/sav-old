{
  "name": "sav-test-fixtures",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run build-client && npm run build-server && npm run build-sass",
    "build-client": "rollup -c ./scripts/build-client.js",
    "build-sass": "node ./scripts/build-sass.js",
    "build-server": "rollup -c ./scripts/build-server.js"
  },
  "devDependencies": {
    "node-sass": "^4.5.3",
    "rollup-standalone": "^0.42.12",
    "vue": "^2.4.2",
    "vue-router": "^2.7.0",
    "vue-server-renderer": "^2.4.2",
    "vue-template-compiler": "^2.4.2"
  }
}
